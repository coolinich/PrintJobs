import { ConvertTimeDurationPipe } from './convert-time-duration.pipe';

describe('ConvertTimeDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTimeDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
